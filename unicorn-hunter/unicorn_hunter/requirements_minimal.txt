# Unicorn Hunter - Minimal Dependencies (Windows compatible)
# Only essential packages for running the service

# Core Framework
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# API Clients
ccxt>=4.1.0
aiohttp>=3.9.0
httpx>=0.25.0

# Utilities
python-dotenv>=1.0.0

# Optional - for Google Gemini AI
# google-generativeai>=0.3.0

# Note: pandas and other data analysis packages are optional
# and can cause issues on Windows. The core service works without them.
